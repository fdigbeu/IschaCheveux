{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeProduit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeProduit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typeProduit.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeproduit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeproduit_edit', { 'id': typeProduit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
